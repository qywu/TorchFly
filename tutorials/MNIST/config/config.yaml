subconfigs:
    - model: base
    - training: base_1gpu
flylogger:
    run:
        # Specify the working directory
        # ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
        dir: ./outputs/${task.name}
task:
    name: mnist-example
    datadir: /tmp
subconfigs:
    - model: base
    - training: base
flylogger:
    run:
        # Specify the working directory
        # ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
        dir: ./outputs/${task.name}
task:
    name: mnist-example
    datadir: /tmp